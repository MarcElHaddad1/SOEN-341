```mermaid
classDiagram
    direction LR

    class LocalStorageManager {
        +LS_KEYS
        +getItem(key)
        +setItem(key,value)
        +removeItem(key)
    }

    class AuthModule {
        +seedOrPatchUsers()
        +getUsers()
        +setUsers(arr)
        +getSession()
        +setSession(user)
        +clearSession()
        +createStudent(email,password,name)
        +createTeacher(email,password,name,code)
        +createAdmin(email,password,name,code)
        +login(email,password,role,code)
    }

    class User {
        +email
        +password
        +role
        +name
    }

    class Session {
        +email
        +role
        +name
    }

    class Event {
        +id
        +title
        +datetime
        +location
        +category
        +org
        +desc
        +cap
        +attendees
        +waitlist
        +createdBy
    }

    class Attendee {
        +email
        +name
        +role
        +ticketId
    }

    class WaitlistEntry {
        +email
        +name
        +role
        +joinedAt
    }

    class EventManager {
        +loadEvents()
        +saveEvents(arr)
        +seedEvents()
        +renderList()
        +renderFilters(events)
        +createEvent(data)
        +claimTicket(eventId,user)
        +joinWaitlist(eventId,user)
        +removeAttendee(eventId,email)
    }

    class QRGenerator {
        +showQRAt(wrapId,canvasId,payload)
        +showQR(payload)
    }

    class UIController {
        +wireGlobal()
        +openModal()
        +closeModal()
        +openNew()
        +closeNew()
        +renderCalendar()
    }

    class CSVExporter {
        +downloadAttendeeCSV(event)
    }

    %% relationships
    LocalStorageManager --> User : stores
    LocalStorageManager --> Session : stores
    LocalStorageManager --> Event : stores
    AuthModule ..> LocalStorageManager : uses
    EventManager ..> LocalStorageManager : uses
    EventManager --> QRGenerator : uses
    EventManager --> CSVExporter : uses
    UIController --> EventManager : interacts
    UIController --> AuthModule : queries
    Event --> "0..*" Attendee : has
    Event --> "0..*" WaitlistEntry : has
```
